{
  "name": "shopify-theme-builder",
  "version": "1.0.0",
  "description": "Build system for Shopify Dawn-based theme",
  "main": "build.js",
  "scripts": {
    "build": "node build.js build",
    "dev": "node build.js watch",
    "clean": "node build.js clean",
    "start": "npm run build && shopify theme serve"
  },
  "keywords": ["shopify", "theme", "dawn", "build"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "fs-extra": "^11.1.1",
    "sass": "^1.69.0",
    "glob": "^10.3.0",
    "chokidar": "^3.5.3"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}